import 'package:flutter/material.dart';

/// Using this to use 70 height in any other component
const double kHeight70 = 70.0;

/// Using this constant to define active color for BMICard component
const Color kCardActiveColor = Color(0xFF1D1E33);

/// Using this constant to define inactive color for BMICard component
const Color kCardInactiveColor = Color(0xFF111228);

/// Using this to use size 18 in font style
const TextStyle kLabelTextStyle18 = TextStyle(
  color: Color(0xFF8D8E98),
  fontSize: 18.0,
);

/// Using this to use size 50 in font style
const TextStyle kLabelTextStyle50 = TextStyle(
  fontSize: 50.0,
);

/// Using this to use size 50 and bold in font style
const TextStyle kLabelTextStyleBold50 = TextStyle(
  fontSize: 50.0,
  fontWeight: FontWeight.w900,
);

const Color kActiveSliderColor = Color(0xFFEB1555);
const Color kInactiveSliderColor = Color(0xFF8D8398);

/// Using this for size material widget BMICircleIconButton
const BoxConstraints kSizeBmiCircleIconButton = BoxConstraints.tightFor(
  width: 56.0,
  height: 56.0,
);

/// Using this for color of BMICircleIconBotton  color
const Color kBmiCircleIconButtonColor = Color(0xFF4C4F5E);
